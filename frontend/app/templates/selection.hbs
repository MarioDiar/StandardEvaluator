<div class="container">
	<div class="row">
		<div class="col-sm-12">
			<div class="container content-container">
			{{#if openCriteriaPicker}}
				{{criteria-picker}}
			{{else}}
				{{main-banner}}
			{{/if}}
			</div>
		</div>
	</div>
</div>
